# Скопируйте этот файл в .env для локального теста (или задайте переменные
# напрямую в панели хостинга при деплое). Реальные значения НИКОГДА не
# коммитьте в git — .env уже добавлен в .gitignore.

YANDEX_API_KEY=вставьте_новый_ключ_сюда
YANDEX_FOLDER_ID=вставьте_folder_id_сюда
